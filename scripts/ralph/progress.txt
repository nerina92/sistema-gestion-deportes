# Ralph Progress Log
Started: 2026-01-13
---

## Codebase Patterns
- Database models use snake_case for field names (mapped with @map in Prisma)
- All monetary values use Decimal type with @db.Decimal(10, 2)
- All tables have createdAt/updatedAt timestamps
- Relations use onDelete: Cascade for items, RESTRICT for references
- Indexes added for foreign keys and frequently queried fields
- API responses use camelCase (Prisma models)
- Frontend expects camelCase property names
- Use 'use client' directive for React components with hooks
- Tailwind CSS for styling, no external CSS files
- All pages under src/app/ follow Next.js 14 App Router conventions

## Important Notes
- Los productos importados desde Excel tienen precios en 0 - necesitan actualización manual
- priceType determina qué campo de precio usar: cash→priceCash, debit→priceDebit, financed→priceFinanced
- Transacciones Prisma con $transaction garantizan atomicidad completa

---

## 2026-01-13 - PRD Update
- PRD actualizado: 17/19 user stories marcadas como completadas
- US pendientes: US-019 (Importación Excel - falta UI), US-013 (Alertas Stock Bajo)
- Se verificó estado real de implementación vs estado registrado en PRD
- ralph.sh adaptado para usar 'claude' en lugar de 'amp'

---

## 2026-01-13 - US-019
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented:
  - Created /productos/importar page with multi-step wizard (upload → preview → result)
  - File upload with format validation (.xlsx/.xls only)
  - Client-side Excel parsing using xlsx library
  - Preview table showing first 50 rows with manual size selector per row
  - Statistics display: valid rows, sold rows (skipped), invalid rows
  - Added "Importar Excel" button (green) to products page header
  - Connected to existing /api/import/excel endpoint
  - Detailed import results with products created, variants created, errors, warnings
- Files changed:
  - src/app/productos/importar/page.tsx (new)
  - src/app/productos/page.tsx (added import button)
- **Learnings for future iterations:**
  - The xlsx library can be dynamically imported on client-side: `const XLSX = await import('xlsx')`
  - Excel column indices: G=6, H=7, I=8, L=11 for prices
  - API at /api/import/excel already handles all backend logic including grouping and normalization
  - TypeScript strict mode requires careful handling of conditional class names to avoid unreachable code
---

## 2026-01-13 - US-013
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- What was implemented:
  - Created /productos/stock-bajo page with low stock variants table
  - Created /api/stock/low endpoint using Prisma raw SQL query
  - Added Stock Bajo link to navigation sidebar (FaExclamationTriangle icon)
  - Table columns: Producto, Variante, SKU, Stock Actual, Stock Min., Diferencia, Estado, Acciones
  - Red badges for stock = 0 (Sin stock), yellow badges for stock <= min (Bajo)
  - "Comprar" button per row links to /compras/nueva with query params for product
  - Statistics cards: total variants, out of stock count, low stock count
  - Results sorted by difference ASC (most critical first)
- Files changed:
  - src/app/api/stock/low/route.ts (new)
  - src/app/productos/stock-bajo/page.tsx (new)
  - src/components/Sidebar.tsx (added Stock Bajo menu item)
- **Learnings for future iterations:**
  - Prisma $queryRaw with template literals allows complex SQL with joins and calculations
  - API routes without request params should use `export async function GET()` (no request argument)
  - Default admin credentials: admin@deporteslaboulaye.com / Admin123!
  - Middleware protects /api/* routes except /api/auth/* and /api/seed-admin
---
